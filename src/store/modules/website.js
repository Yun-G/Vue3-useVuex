const state = {
};
// getters 从 store 中的 state 中派生出一些状态
const getters = {};
// actions
const actions = {

};
// mutations store.commit改变state,唯一改变state的地方
const mutations = {

};

export default {
	namespaced: true,
	state,
	getters,
	actions,
	mutations
};
